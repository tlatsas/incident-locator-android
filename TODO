TODO
----
* show version number inside the application
* show camera image when registering a new incident
* show camera overlay to assist user targeting
* compensate for compass offset in devices with buggy kernels [major]
